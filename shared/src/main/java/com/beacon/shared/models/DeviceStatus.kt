package com.beacon.shared.models

enum class DeviceStatus {
    ONLINE,
    OFFLINE,
    UNKNOWN,
    INACTIVE,
    GREEN_LIVE,
    BLUE_INTERVAL,
    YELLOW_IDLE,
    RED_OFFLINE,
    GRAY_UNPAIRED
}
